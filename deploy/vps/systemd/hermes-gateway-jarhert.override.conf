[Service]
# Let an active Telegram turn finish before a profile sync restarts the
# gateway. A hard cap still recovers a genuinely stuck run.
Environment=HERMES_RESTART_DRAIN_TIMEOUT=60
TimeoutStopSec=90s
